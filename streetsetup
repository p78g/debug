--[[
    Script: Auto Buy & Equip SkiMask
    - Withdraw 5050
    - Respawn
    - Teleport to Gun Store (1053, 54, -104)
    - Activate ProPad ProximityPrompt
    - Buy SkiMask (50)
    - Equip mask (MASKED.RemoteEvent)
    No teleport back.
]]

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer

-- Helper: wait with delay
local function Wait(delay)
    task.wait(delay or 0.3)
end

-- Helper: get current character and its HumanoidRootPart
local function GetCharacter()
    local char = LocalPlayer.Character
    if not char or not char.Parent then
        char = LocalPlayer.CharacterAdded:Wait()
    end
    local rootPart = char:WaitForChild("HumanoidRootPart", 10)
    return char, rootPart
end

-- --- Save original position (not used later, but kept for reference)
local char, rootPart = GetCharacter()
local originalPos = rootPart.Position
print("Original position saved:", originalPos)

-- --- Get remote references
local remotes = ReplicatedStorage:WaitForChild("Remotes", 10)
local ATM = remotes:WaitForChild("ATM", 10)
local ControllerRemote = remotes:WaitForChild("ControllerRemote", 10)
local BuyRemote = remotes:WaitForChild("Buy", 10)

-- --- Step 1: Withdraw
print("Step 1: Withdraw 5050")
ATM:FireServer("Withdraw", 5050)
Wait(0.3)

-- --- Step 2: Respawn
print("Step 2: Respawn")
ControllerRemote:FireServer("/respawn")
Wait(1.5) -- give time for respawn to complete

-- --- Step 3: Get new character after respawn
print("Step 3: Getting new character")
char, rootPart = GetCharacter()
print("New character loaded")

-- --- Step 4: Teleport to Gun Store
local gunStorePos = Vector3.new(1053, 54, -104)
print("Step 4: Teleport to Gun Store")
rootPart.CFrame = CFrame.new(gunStorePos)
Wait(0.5)

-- --- Step 5: Activate ProPad ProximityPrompt
print("Step 5: Activating ProximityPrompt")
local map = workspace:WaitForChild("Map", 10)
local gunStore = map:WaitForChild("GunStore", 10)
local gunTable = gunStore:WaitForChild("GunTable", 10)
local proPad = gunTable:WaitForChild("ProPad", 10)
local handle = proPad:WaitForChild("Handle", 10)
local prompt = handle:WaitForChild("ProximityPrompt", 10)

-- Move exactly onto the handle to guarantee proximity
local handlePos = handle.Position
rootPart.CFrame = CFrame.new(handlePos.X, handlePos.Y + 2, handlePos.Z)
Wait(0.2)

-- Fire the prompt
local promptFired = false
local success, err = pcall(function()
    prompt:Prompt()
end)
if success then
    print("Prompt fired via Prompt()")
    promptFired = true
else
    warn("Prompt() failed:", err)
    pcall(function()
        prompt:InputHoldBegin()
        Wait(0.15)
        prompt:InputHoldEnd()
        print("Prompt fired via InputHold")
        promptFired = true
    end)
end

if not promptFired then
    warn("ProximityPrompt activation failed. The store may not open.")
end

-- Wait for the store interface to open
Wait(0.5)

-- --- Step 6: Buy SkiMask
print("Step 6: Buying SkiMask (50)")
BuyRemote:FireServer("SkiMask", 50)
Wait(0.5) -- allow purchase to be processed

-- --- Step 7: Equip mask
print("Step 7: Equipping mask")

-- Wait for MASKED folder to appear (it is added after buying the mask)
local maskedFolder = char:FindFirstChild("MASKED")
if not maskedFolder then
    maskedFolder = char:WaitForChild("MASKED", 5)
end

if maskedFolder then
    local maskRemote = maskedFolder:FindFirstChild("RemoteEvent")
    if not maskRemote then
        maskRemote = maskedFolder:WaitForChild("RemoteEvent", 5)
    end
    if maskRemote then
        maskRemote:FireServer("Equip")
        print("Equip remote fired")
    else
        warn("RemoteEvent not found in MASKED folder")
    end
else
    warn("MASKED folder not found – mask may not be in inventory")
end

print("All steps completed.")--[[
    Robust Automation Script
    Steps:
    1. Withdraw 5050
    2. Respawn
    3. Teleport to Gun Store
    4. Fire ProximityPrompt (reliably)
    5. Teleport back
    6. Buy SkiMask (50)
    7. Equip mask
]]

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer

-- Helper: wait with delay
local function Wait(delay)
    task.wait(delay or 0.3)
end

-- Helper: get character and humanoid root part
local function GetCharacter()
    local char = LocalPlayer.Character
    if not char or not char.Parent then
        char = LocalPlayer.CharacterAdded:Wait()
    end
    local rootPart = char:WaitForChild("HumanoidRootPart", 10)
    return char, rootPart
end

-- Save original position before respawn
local char, rootPart = GetCharacter()
local originalPos = rootPart.Position
print("Original position saved:", originalPos)

-- Get remote references
local remotes = ReplicatedStorage:WaitForChild("Remotes", 10)
local ATM = remotes:WaitForChild("ATM", 10)
local ControllerRemote = remotes:WaitForChild("ControllerRemote", 10)
local BuyRemote = remotes:WaitForChild("Buy", 10)

-- Step 1: Withdraw
print("Step 1: Withdrawing 5050")
ATM:FireServer("Withdraw", 5050)
Wait(0.3)

-- Step 2: Respawn
print("Step 2: Respawning")
ControllerRemote:FireServer("/respawn")
Wait(1.5) -- extra time for respawn to complete

-- Step 3: Get new character after respawn
print("Step 3: Getting new character after respawn")
char, rootPart = GetCharacter()
print("New character loaded")

-- Step 4: Teleport to Gun Store
local gunStorePos = Vector3.new(1053, 54, -104)
print("Step 4: Teleporting to Gun Store", gunStorePos)
rootPart.CFrame = CFrame.new(gunStorePos)
Wait(0.5) -- allow teleport to settle

-- Step 5: Fire proximity prompt
print("Step 5: Attempting to fire ProximityPrompt")

-- Get the prompt and its handle
local map = workspace:WaitForChild("Map", 10)
local gunStore = map:WaitForChild("GunStore", 10)
local gunTable = gunStore:WaitForChild("GunTable", 10)
local proPad = gunTable:WaitForChild("ProPad", 10)
local handle = proPad:WaitForChild("Handle", 10)
local prompt = handle:WaitForChild("ProximityPrompt", 10)

-- Move exactly onto the handle to ensure proximity
local handlePos = handle.Position
rootPart.CFrame = CFrame.new(handlePos.X, handlePos.Y + 2, handlePos.Z)
Wait(0.2)

-- Fire the prompt (method 1)
local promptFired = false
local success, err = pcall(function()
    prompt:Prompt()
end)
if success then
    print("Prompt fired via Prompt()")
    promptFired = true
else
    warn("Prompt() failed:", err)
    -- Fallback: simulate input hold
    pcall(function()
        prompt:InputHoldBegin()
        Wait(0.15)
        prompt:InputHoldEnd()
        print("Prompt fired via InputHold")
        promptFired = true
    end)
end

if not promptFired then
    warn("All prompt methods failed. The store may not open.")
end

-- Wait for the store interaction to complete (purchase UI, etc.)
Wait(0.5)

-- Step 6: Teleport back to original position
print("Step 6: Returning to original position")
rootPart.CFrame = CFrame.new(originalPos.X, originalPos.Y, originalPos.Z)
Wait(0.3)

-- Step 7: Buy SkiMask
print("Step 7: Buying SkiMask (50)")
BuyRemote:FireServer("SkiMask", 50)
Wait(0.3)

-- Step 8: Equip mask – wait for MASKED folder if necessary
print("Step 8: Equipping mask")
local maskedFolder = char:FindFirstChild("MASKED")
if not maskedFolder then
    maskedFolder = char:WaitForChild("MASKED", 5)
end
if maskedFolder then
    local maskRemote = maskedFolder:FindFirstChild("RemoteEvent")
    if not maskRemote then
        maskRemote = maskedFolder:WaitForChild("RemoteEvent", 5)
    end
    if maskRemote then
        maskRemote:FireServer("Equip")
        print("Equip remote fired")
    else
        warn("RemoteEvent not found in MASKED folder")
    end
else
    warn("MASKED folder not found after purchase")
end

print("All steps completed.")
