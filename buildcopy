-- =============================================
-- BUILD COPIER – Pure Remote Calls + Unique Part Naming
-- Real offsets default to 0,0,0 (inside your plot)
-- Ghost offsets use your measured values
-- =============================================

local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
    Name = "Build Copier",
    ConfigurationSaving = { Enabled = true, FolderName = "BuildCopier" },
    Theme = "Default",
})

local MainTab = Window:CreateTab("Copy Build")
MainTab:CreateSection("Ghost Preview Offsets (visual only)")

local ghostX = -28.637
local ghostZ = -17.512
local ghostYextra = 0
local baseHeight = 5

MainTab:CreateSlider({ Name = "Ghost X Offset", Range = {-100, 100}, Increment = 0.5, CurrentValue = ghostX, Callback = function(v) ghostX = v end })
MainTab:CreateSlider({ Name = "Ghost Z Offset", Range = {-100, 100}, Increment = 0.5, CurrentValue = ghostZ, Callback = function(v) ghostZ = v end })
MainTab:CreateSlider({ Name = "Ghost Extra Y", Range = {-10, 10}, Increment = 0.5, CurrentValue = ghostYextra, Callback = function(v) ghostYextra = v end })

MainTab:CreateSection("Real Copy Offsets (actual placement)")

local realX = 0
local realZ = 0
local realYextra = 0

MainTab:CreateSlider({ Name = "Real X Offset", Range = {-100, 100}, Increment = 0.5, CurrentValue = realX, Callback = function(v) realX = v end })
MainTab:CreateSlider({ Name = "Real Z Offset", Range = {-100, 100}, Increment = 0.5, CurrentValue = realZ, Callback = function(v) realZ = v end })
MainTab:CreateSlider({ Name = "Real Extra Y", Range = {-10, 10}, Increment = 0.5, CurrentValue = realYextra, Callback = function(v) realYextra = v end })

MainTab:CreateButton({
    Name = "Reset Real Offsets to 0",
    Callback = function() realX = 0; realZ = 0; realYextra = 0; Rayfield:Notify({Title = "Reset", Content = "Real offsets = 0,0,0"}) end
})

MainTab:CreateSection("Player Selection")

local selectedPlayer = nil
local playerMapping = {}
local dropdown = nil

local function getBuildArea(folderName)
    local pa = workspace:FindFirstChild("Private Building Areas")
    return pa and pa:FindFirstChild(folderName)
end

local function refreshPlayers()
    local pa = workspace:FindFirstChild("Private Building Areas")
    if not pa then return {} end
    local opts = {}
    playerMapping = {}
    for _, c in ipairs(pa:GetChildren()) do
        local name = c.Name
        if name:sub(-9) == "BuildArea" then
            local pn = name:sub(1, -10)
            table.insert(opts, pn)
            playerMapping[pn] = name
        end
    end
    return opts
end

local function clearLocalBuild()
    local localName = game.Players.LocalPlayer.Name .. "BuildArea"
    local localBA = getBuildArea(localName)
    if localBA and localBA:FindFirstChild("Build") then
        for _, p in ipairs(localBA.Build:GetChildren()) do p:Destroy() end
        Rayfield:Notify({ Title = "Cleared", Content = "Your Build folder emptied." })
    end
end

-- Auto‑centre of source build (average of all part positions)
local function computeSourceCentre(buildArea)
    local buildFolder = buildArea:FindFirstChild("Build")
    if not buildFolder then return buildArea.Position end
    local parts = {}
    local function collect(obj)
        for _, child in ipairs(obj:GetChildren()) do
            if child:IsA("BasePart") then
                table.insert(parts, child)
            elseif child:IsA("Model") then
                collect(child)
            end
        end
    end
    collect(buildFolder)
    if #parts == 0 then return buildArea.Position end
    local sum = Vector3.new(0,0,0)
    for _, p in ipairs(parts) do sum = sum + p.Position end
    return sum / #parts
end

-- Remote access
local remote = nil
local function getRemote()
    if remote then return remote end
    local char = game.Players.LocalPlayer.Character
    if not char then return nil end
    local bt = char:FindFirstChild("Building Tools")
    if not bt then return nil end
    local api = bt:FindFirstChild("SyncAPI")
    if not api then return nil end
    remote = api:FindFirstChild("ServerEndpoint")
    return remote
end

local function vec(v) return vector.create(v.X, v.Y, v.Z) end

-- Copy a single part using remote calls, with unique temporary naming
local function copySinglePart(part, srcOrigin, dstOrigin, dstBuild)
    local typeMap = {
        Part = "Normal", Truss = "Truss", Wedge = "Wedge", Corner = "Corner",
        Cylinder = "Cylinder", Ball = "Ball", Seat = "Seat",
        ["Vehicle Seat"] = "Vehicle Seat", Spawn = "Spawn"
    }
    local partType = typeMap[part.Name] or "Normal"

    local offset = part.Position - srcOrigin
    local targetPos = dstOrigin + offset
    local targetCFrame = CFrame.new(targetPos) * (part.CFrame - part.CFrame.Position)

    local r = getRemote()
    if not r then return nil end

    -- Create part at destination origin
    local createArgs = {"CreatePart", partType, CFrame.new(dstOrigin), dstBuild}
    local ok, newPart = pcall(function() return r:InvokeServer(unpack(createArgs)) end)
    if not ok or not newPart then return nil end
    task.wait(0.1)

    local actualPart = newPart
    if not actualPart:IsA("BasePart") then actualPart = dstBuild:FindFirstChild(part.Name) end
    if not actualPart then return nil end

    -- Give it a unique temporary name so server can differentiate
    local originalName = actualPart.Name
    local uniqueName = "Copy_" .. os.clock() .. "_" .. originalName
    actualPart.Name = uniqueName
    task.wait(0.05)

    -- Rotate
    local pivotCFrame = targetCFrame - targetCFrame.Position
    local currentCFrame = CFrame.new(dstOrigin)
    local rotArgs = {
        "SyncRotate",
        { { Part = actualPart, CFrame = targetCFrame } },
        { "AroundPivot", pivotCFrame, "Center", currentCFrame, "Center" }
    }
    pcall(function() r:InvokeServer(unpack(rotArgs)) end)
    task.wait(0.5)

    -- Move
    local delta = targetPos - dstOrigin
    local moveArgs = {
        "SyncMove",
        { { Part = actualPart, CFrame = CFrame.new(targetPos) } },
        { "AlongFace", vec(delta), "Global", CFrame.new(targetPos) }
    }
    pcall(function() r:InvokeServer(unpack(moveArgs)) end)
    task.wait(0.5)

    -- Resize
    local resizeArgs = {
        "SyncResize",
        { { Part = actualPart, CFrame = CFrame.new(targetPos), Size = vec(part.Size) } },
        { "ResizeFace", Enum.NormalId.Front, 1, vec(Vector3.new(0,0,0)), "Z", part.Size, "Normal" }
    }
    pcall(function() r:InvokeServer(unpack(resizeArgs)) end)
    task.wait(0.5)

    -- Restore original name (optional)
    actualPart.Name = originalName

    return actualPart
end

-- Main copy function using remote calls with real offsets
local function copyRealBuild()
    if not selectedPlayer then
        Rayfield:Notify({ Title = "Error", Content = "Select a player." })
        return
    end

    local srcFolder = playerMapping[selectedPlayer]
    local srcBA = getBuildArea(srcFolder)
    local dstBA = getBuildArea(game.Players.LocalPlayer.Name .. "BuildArea")
    if not srcBA or not dstBA then
        Rayfield:Notify({ Title = "Error", Content = "BuildAreas missing." })
        return
    end

    local srcBuild = srcBA:FindFirstChild("Build")
    local dstBuild = dstBA:FindFirstChild("Build")
    if not srcBuild or not dstBuild then
        Rayfield:Notify({ Title = "Error", Content = "Build folders missing." })
        return
    end

    -- Compute source centre
    local srcCentre = computeSourceCentre(srcBA)
    local finalY = baseHeight + realYextra
    local srcOrigin = srcCentre + Vector3.new(0, finalY, 0)
    local dstOrigin = dstBA.Position + Vector3.new(realX, finalY, realZ)

    -- Collect all BaseParts recursively
    local allParts = {}
    local function collect(obj)
        for _, child in ipairs(obj:GetChildren()) do
            if child:IsA("BasePart") then
                table.insert(allParts, child)
            elseif child:IsA("Model") then
                collect(child)
            end
        end
    end
    collect(srcBuild)
    if #allParts == 0 then
        Rayfield:Notify({ Title = "Info", Content = "No parts found." })
        return
    end

    clearLocalBuild()
    task.wait(0.5)

    Rayfield:Notify({ Title = "Copying", Content = #allParts .. " parts..." })
    local successCount = 0
    for _, part in ipairs(allParts) do
        if copySinglePart(part, srcOrigin, dstOrigin, dstBuild) then
            successCount = successCount + 1
        end
        task.wait(0.1)
    end
    Rayfield:Notify({ Title = "Complete", Content = successCount .. "/" .. #allParts .. " parts copied." })
end

-- Ghost preview (direct clones, no remote, uses ghost offsets)
local ghostModel = nil
local function showGhost()
    if ghostModel then ghostModel:Destroy() end
    if not selectedPlayer then
        Rayfield:Notify({ Title = "Error", Content = "Select a player." })
        return
    end

    local srcFolder = playerMapping[selectedPlayer]
    local srcBA = getBuildArea(srcFolder)
    local dstBA = getBuildArea(game.Players.LocalPlayer.Name .. "BuildArea")
    if not srcBA or not dstBA then
        Rayfield:Notify({ Title = "Error", Content = "BuildAreas missing." })
        return
    end
    local srcBuild = srcBA:FindFirstChild("Build")
    if not srcBuild then
        Rayfield:Notify({ Title = "Error", Content = "Source has no Build folder." })
        return
    end

    local srcCentre = computeSourceCentre(srcBA)
    local finalY = baseHeight + ghostYextra
    local srcOrigin = srcCentre + Vector3.new(0, finalY, 0)
    local dstOrigin = dstBA.Position + Vector3.new(ghostX, finalY, ghostZ)

    ghostModel = Instance.new("Model")
    ghostModel.Name = "GhostPreview"

    local function cloneParts(obj)
        for _, child in ipairs(obj:GetChildren()) do
            if child:IsA("BasePart") then
                local offset = child.Position - srcOrigin
                local targetPos = dstOrigin + offset
                local clone = child:Clone()
                clone.Position = targetPos
                clone.CFrame = CFrame.new(targetPos) * (child.CFrame - child.CFrame.Position)
                clone.Anchored = true
                clone.CanCollide = false
                clone.Transparency = 0.7
                clone.Material = Enum.Material.ForceField
                clone.Parent = ghostModel
            elseif child:IsA("Model") then
                cloneParts(child)
            end
        end
    end
    cloneParts(srcBuild)
    ghostModel.Parent = workspace
    Rayfield:Notify({ Title = "Ghost Preview", Content = "Ghost uses its own offsets. Real copy uses separate offsets (default 0)." })
end

local function hideGhost()
    if ghostModel then ghostModel:Destroy() end
end

-- Place a marker at real destination origin
local function showRealMarker()
    if not selectedPlayer then
        Rayfield:Notify({ Title = "Error", Content = "Select a player." })
        return
    end
    local srcBA = getBuildArea(playerMapping[selectedPlayer])
    local dstBA = getBuildArea(game.Players.LocalPlayer.Name .. "BuildArea")
    if not dstBA then return end
    local finalY = baseHeight + realYextra
    local dstOrigin = dstBA.Position + Vector3.new(realX, finalY, realZ)
    local marker = Instance.new("Part")
    marker.Size = Vector3.new(1, 1, 1)
    marker.Color = Color3.fromRGB(0, 255, 0)
    marker.Material = Enum.Material.Neon
    marker.Position = dstOrigin
    marker.Anchored = true
    marker.CanCollide = false
    marker.Parent = workspace
    game:GetService("Debris"):AddItem(marker, 5)
    Rayfield:Notify({ Title = "Marker", Content = "Green marker at REAL copy origin (real offsets)." })
end

-- UI
local initOpts = refreshPlayers()
dropdown = MainTab:CreateDropdown({
    Name = "Select Player",
    Options = initOpts,
    CurrentOption = initOpts[1] or "",
    Callback = function(opt) selectedPlayer = opt end
})
if initOpts[1] then selectedPlayer = initOpts[1] end

MainTab:CreateButton({ Name = "Clear Your Build", Callback = clearLocalBuild })
MainTab:CreateButton({ Name = "Show Ghost Preview", Callback = showGhost })
MainTab:CreateButton({ Name = "Hide Ghost", Callback = hideGhost })
MainTab:CreateButton({ Name = "Show Real Destination Marker", Callback = showRealMarker })
MainTab:CreateButton({ Name = "Copy Real Build (Remote Calls)", Callback = copyRealBuild })
MainTab:CreateButton({ Name = "Refresh Players", Callback = function()
    local newOpts = refreshPlayers()
    dropdown:SetOptions(newOpts)
    if newOpts[1] then dropdown:SetValue(newOpts[1]); selectedPlayer = newOpts[1] end
end })

Rayfield:Notify({ Title = "Ready", Content = "Ghost uses your measured offsets. Real copy uses separate offsets (default 0). Use marker to verify." })
